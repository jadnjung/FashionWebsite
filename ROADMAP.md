# Esque — Roadmap

Tracks Collection 001 / V1 build order, near-term follow-ups (V1.1), and deferred ideas (V2+). See [PROJECT.md §91–92](./PROJECT.md#91-v1) for the full required/deferred feature lists and [DESIGN_SYSTEM.md §79–80](./DESIGN_SYSTEM.md#79-build-order) for the build-order rationale (functionality before spectacular animation).

Status legend: `[ ]` not started · `[~]` in progress · `[x]` done

## Phase 0 — Project Foundation

- [ ] Create Shopify development store — blocked on the project owner's Shopify account setup; not an implementation task
- [x] Scaffold Next.js (App Router, TypeScript) app on pnpm
- [ ] Connect Vercel project + preview deployments — blocked on the project owner's Vercel account setup; not an implementation task
- [x] Set up environment variable scaffolding (see [ARCHITECTURE.md §7](./ARCHITECTURE.md#7-environment-variables-draft--fill-in-once-the-shopify-store-exists))
- [x] Wire up CI (lint, typecheck, Playwright) — plus format:check and build; no deploy step yet (no Vercel project)

## Phase 1 — Design System / Tokens

- [x] Implement color tokens ([DESIGN_SYSTEM.md §4](./DESIGN_SYSTEM.md#4-core-color-system))
- [x] Implement type scale + two-typeface system ([DESIGN_SYSTEM.md §6–9](./DESIGN_SYSTEM.md#6-typography-system))
- [~] Implement spacing scale + responsive grid ([DESIGN_SYSTEM.md §13–15](./DESIGN_SYSTEM.md#13-spacing-system)) — spacing scale implemented (Tailwind `--spacing` base unit reproduces the 4/8/12/16/24/32/48/64/96/128 scale); the 12/8/4-column responsive grid remains conceptual/undocumented-in-code until Phase 4/5's catalog and PDP layouts give it real content to implement and validate against
- [~] Build base components: buttons, inputs, form fields — Button implemented (primary/secondary/editorial variants); Input and other form fields deliberately deferred (YAGNI — no real consumer exists until Phase 4/6/10's search, request-access, and account forms)

## Phase 2 — Commerce Foundation

- [x] Shopify Storefront API client + typed GraphQL queries
- [x] Product/collection data fetching
- [ ] Cart (Storefront API cart object, persisted cart ID) — deferred pending a real Shopify store; see DECISIONS.md D-016
- [ ] Shopify Checkout handoff — deferred pending a real Shopify store; see DECISIONS.md D-016

## Phase 3 — Core Storefront Shell

- [x] Global layout, header (`ESQUE MENU SEARCH ACCOUNT BAG`) — including a mobile-responsive utility nav (icon-compressed SEARCH/ACCOUNT below `md`, see DECISIONS.md D-014)
- [x] Full-screen experimental menu — focus trap, Escape-to-close, closes on navigation, reduced-motion support
- [x] Footer
- [x] Base page transitions — the full-screen menu's open/close is the one transition that exists in this pass (the homepage placeholder is still a single route); cross-page shared-element transitions are deferred to whichever later phase first introduces a second route (see DECISIONS.md D-013)

## Phase 4 — Catalog

- [x] New / Tops / Bottoms / Etc. category pages — seven routes (`/new`, `/tops`(+`/[subcategory]`), `/bottoms`(+`/[subcategory]`), `/etc`(+`/[subcategory]`)), server-rendered against the typed Shopify client; see DECISIONS.md D-023 for the category→product-type query architecture. An unknown subcategory shows the correct branded 404 UI but currently returns HTTP 200 + `noindex` rather than a hard 404 — investigated and deferred to Phase 12's SEO pass, see DECISIONS.md D-026
- [~] Search (predictive, full-screen overlay) — resolves D-025's deferral: a real full-screen overlay (`components/navigation/SearchOverlay.tsx`) triggered from Header's `SEARCH` button, a debounced (200ms) input querying Shopify's `predictiveSearch` root query (this codebase's first Route Handler, `app/api/search/route.ts`) for PRODUCTS results and a local, zero-I/O taxonomy match (`lib/catalog/taxonomy.ts`'s `searchCategories`) for CATEGORIES results, reusing `ProductCard`/`FullScreenMenu`'s proven accessibility mechanics, with real loading/empty (`NOTHING MATCHES.`)/error/populated states and `trackEvent('search', ...)` wired for PROJECT.md §82's "Search queries, no-result searches." Demo Mode extended so the project owner's local preview can demonstrate real, populated results. Left `[~]`, honestly: DESIGN_SYSTEM.md §50's third result group, COLLECTIONS, is deliberately not built (no `/collections/[handle]` route exists to link to, and only one real collection exists in the launch catalog — the same reasoning D-024 already used to defer a Collection filter), and — the same limitation D-025 itself named and this pass could not change — relevance/ranking quality against a real, populated catalog remains unverifiable without one. See DECISIONS.md D-058.
- [~] Filters (category, size, color, availability, collection, price, sort) — Category/Subcategory satisfied by routing; Sort (all four PROJECT.md §34 options), Availability, and Price range implemented as zero-JavaScript URL-driven filters, fixture-tested against the typed client; Size, Color, and Collection deferred — see DECISIONS.md D-024
- [x] Product grid (unconventional layout per [DESIGN_SYSTEM.md §37–38](./DESIGN_SYSTEM.md#37-collection-page)) — periodic featured/standard layout across the now-implemented 12/8/4-column responsive grid; the editorial-image insert from DESIGN_SYSTEM's illustrative example sequence is deferred until real campaign photography exists to place there

## Phase 5 — Product Detail Page

- [x] PDP layout (60/40 media/info split) — real, server-rendered `/products/[handle]` route (`ProductDetail`/`ProductGallery`/`ProductPurchasePanel`), gallery `lg:col-span-7` / sticky purchase panel `lg:col-span-5` on the existing 12-column grid, single column below `lg`
- [x] Size/color selection, size guide — generic, data-driven selector (native radio groups, D-010 focus rings) against real `product.options`/`variants` (now fetched — see DECISIONS.md D-028); Size Guide is a real native-`<dialog>` panel (side panel desktop / bottom sheet mobile) with clearly-labeled placeholder measurements — final sizes are still an open product decision (PROJECT.md §101) — see DECISIONS.md D-027
- [~] Quick Add (desktop overlay + mobile bottom sheet) — resolves D-029: the on-grid entry point is now real and unconditional on every `ProductCard` (category grids, PDP Related Products, Search results, homepage Selected Pieces), via a new on-demand `GET /api/products/[handle]` Route Handler (mirroring D-058's search Route Handler) feeding the existing `VariantPicker`/`lib/product/variants.ts` machinery into a `SizeGuidePanel`-style native-`<dialog>` (D-027) — a centered lightweight overlay on desktop, a bottom sheet on mobile; real loading/error/not-found/sold-out states; `ADD TO BAG` is a real, disabled-until-complete, no-op-`onClick` stub matching the PDP's own precedent (D-016/D-029); `quick_add_open` added to the analytics taxonomy alongside reused `quick_add_click`/`variant_selected` events — see DECISIONS.md D-060. Left `[~]`, honestly, for the same reason as this phase's other lines: no real Shopify store exists in this environment, so a permanent, committed E2E test of the real trigger→fetch→add flow through ordinary navigation isn't possible here — verified instead via a temporary fixture probe and a live Demo Mode pass. An independent review during this pass also found and fixed a genuine, previously-undetected mobile bottom-sheet positioning defect in the already-shipped `SizeGuidePanel`/`ShopTheLookPanel` components — see DECISIONS.md D-059.
- [~] Scarcity UI (low stock / final pieces — real inventory only) — real, fixture-tested display-tier logic against Shopify's actual `quantityAvailable`; renders nothing when the count is unknown rather than fabricating one; whether a real store ever populates a non-null value is unverifiable without one — see DECISIONS.md D-028
- [~] Related products / Complete the Look / Recently viewed — Related Products (mechanically derived, same product type, reusing the existing `ProductGrid`) and Recently Viewed (versioned `localStorage`, `useSyncExternalStore`) built; Complete the Look deferred (needs real curatorial outfit-pairing data that doesn't exist) — see DECISIONS.md D-030/D-031

## Phase 6 — Access Gate

- [x] Password entry UI + branded incorrect-password microcopy
- [x] Access cookie (~30 day persistence), separate VIP/early-access claim
- [~] Request Access form → Klaviyo list + password email — form submits to Klaviyo's list-subscription API with marketing consent (see DECISIONS.md D-020); the transactional password-delivery email itself is not yet built, since it needs a real Klaviyo account/email template that doesn't exist yet (same dependency D-016 already noted for Shopify checkout)
- [x] Confirm SEO rule: product/collection routes stay crawlable regardless of access state ([DECISIONS.md D-005](./DECISIONS.md#d-005--access-gate-is-a-ui-layer-experience-not-an-seo-wall)) — verified architecturally (crawler bypass + homepage placeholder), since no real product/collection page exists yet (Phase 4/5)

## Phase 7 — Homepage

- [~] Scene 01: Collection Hero — real structure, copy slots, and `ENTER COLLECTION` CTA built; hero imagery and the campaign statement are clearly-labeled placeholders (no real photography/campaign direction exists yet — PROJECT.md §101); cursor-driven depth motion (DESIGN_SYSTEM.md §27) deferred whole-cloth to ROADMAP.md Phase 9's own "Parallax / depth on homepage scenes" — see DECISIONS.md D-032
- [x] Scene 02: Interactive Model — the real feature now exists (Phase 8, below): hotspots, a live product info panel, and Shop the Look. `InteractiveModelPlaceholder` (this phase's original placeholder-slot build) is retained as Phase 8's own honest fallback for when Shopify hasn't resolved a real product for every hotspot region, rather than being discarded — see DECISIONS.md D-034
- [x] Scene 03: Collection Statement — fully built, real copy (DESIGN_SYSTEM.md §32), zero Shopify dependency
- [~] Scene 04: Selected Pieces — real, Shopify-backed mechanism (up to 3 real products via a page-isolated `getSelectedPieces`, reusing the existing `ProductCard`); renders whatever photography Shopify has (none yet); the entire section is omitted when no products are available (unconfigured store today) rather than fabricating content — see DECISIONS.md D-033
- [x] Scene 05: Categories — fully built, real taxonomy-backed navigation (TOPS/BOTTOMS/ETC., linking to the real Phase 4 category routes)
- [x] Scene 06: Drop Status — fully built, real copy verbatim from DESIGN_SYSTEM.md §35/CONTENT.md §8; piece count is static (PROJECT.md §8's committed 6-piece launch catalog) rather than live-queried — same "only one real collection exists to prove a query isn't just hardcoding" reasoning as D-030 — see DECISIONS.md D-032
- [ ] Scene 07: Archive Preview (minimal/hidden until Collection 002 exists) — omitted this pass; no past collection exists yet to preview, `/archive` doesn't exist until Phase 11, and DESIGN_SYSTEM.md §58's own mobile homepage sequence already excludes it — matches this line's own "minimal/hidden" allowance — see DECISIONS.md D-032

## Phase 8 — Interactive Model (signature feature)

- [x] Stage 1: hotspots work (silhouette-based hit regions, not rectangles) — two real `<button>` elements, percentage-based `clip-path: polygon()` (silhouette-shaped, not rectangular; hit-region and visible shape are identical at every viewport width), each bound to one real Shopify product via the existing product-type query mechanism (D-023); built against a hand-authored placeholder illustration — see DECISIONS.md D-034/D-035. D-035's "clicking toggles the panel" description was corrected by ROADMAP.md Phase 9's D-043 (a mouse click could never actually reach the toggle-closed branch — see below); D-039 additionally resolved D-034/D-035's own deferred click-vs-navigate question by wiring the panel's `VIEW PRODUCT` link to a shared-element transition rather than changing hotspot activation itself.
- [x] Stage 2: product info panel works (name, category, price, colors, sizes, View/Quick Add) — real, live product data (`getProduct`), real variant selection (shared `VariantPicker`, D-036) with live availability gating, `VIEW PRODUCT` (real navigation, now shared-transition-enhanced per Phase 9's D-039) and `QUICK ADD` (real, disabled-until-complete, no-op `onClick` — no cart exists yet, D-016/D-029). An independent review pass found and this phase fixed a real keyboard-reachability defect and a Shop the Look sold-out gap before completion — see DECISIONS.md D-037
- [x] Stage 3: responsive/mobile tap behavior works — mouse hover previews a region, click/tap/Enter/Space activates it (native `<button>` semantics give touch and keyboard activation for free); focus alone deliberately does NOT preview, corrected from the original "focus also previews" design after it was found to make one hotspot's panel unreachable by sequential Tab — see DECISIONS.md D-037. A second independent review during ROADMAP.md Phase 9 found that activation itself was, in practice, unreachable by mouse click (hover always primed the state a click would then immediately toggle back off) — fixed to an unconditional activate rather than a toggle; see DECISIONS.md D-043.
- [~] Stage 4: motion added (masked luminance highlight, not glow) — deferred: this is a photographic treatment with nothing real to mask against an abstract placeholder illustration; Stage 1-3 use a plain, functional CSS color/opacity transition instead (automatically reduced-motion-safe via the existing sitewide rule) — see DECISIONS.md D-034
- [ ] Stage 5: visual refinement — deferred alongside Stage 4; presupposes both motion and real photography to refine against, neither of which exists yet
- [x] Shop the Look panel + multi-item add flow — native `<dialog>` (D-027's `SizeGuidePanel` pattern), lists exactly the same real, per-region resolved garments hotspots already show (no separate curation data needed — see DECISIONS.md D-034), per-item include/exclude checkbox + real variant selection, `ADD LOOK` disabled until every included item's selection is complete (PROJECT.md §29)

## Phase 9 — Advanced Motion

- [x] Custom cursor (desktop only, with accessibility fallback) — vanilla React/DOM (no animation library), gated on fine-pointer-and-hover, reduced motion, and a best-effort low-performance-device heuristic; never hides the OS cursor before a real pointermove is observed; hidden over form fields; scoped to the storefront shell only (the access gate keeps its own existing `EntranceMotion` pointer treatment) — see DECISIONS.md D-040
- [x] Shared-element product transitions (grid → PDP, interactive model → PDP) — React's `<ViewTransition>` (native browser View Transitions API), resolving D-013's deferral; wraps `ProductCard`/`ProductGallery`'s image containers (real today even without product photography) and each Interactive Model hotspot, keyed by product handle; visual payoff is only fully realized once a real, reasonably fast-responding Shopify store exists (this dev/CI environment has none) — see DECISIONS.md D-038. Also resolves D-034/D-035's deferred click-vs-navigate question for the Interactive Model: the hover/activate interaction is unchanged, and the panel's `VIEW PRODUCT` link now carries the transition — see DECISIONS.md D-039
- [x] Parallax / depth on homepage scenes — Scene 01 (Hero) only, picking up D-032's deferral; vanilla CSS-custom-property mechanism (no animation library, deliberately not repeating `EntranceMotion`'s ~119KB-on-`/access` pattern on the homepage's own critical route). Scenes 02, 03, 04, 05, 06 were each individually considered and declined — no imagery to create depth against, or an active usability cost — see DECISIONS.md D-041
- [x] Reduced-motion audit across all of the above — every pre-existing animated surface (menu, access-gate entrance/error motion, hero reveal) confirmed already correct; this phase's own new work built reduced-motion-safe from the start; one real gap found and fixed (the sitewide universal-selector rule doesn't reach the browser's `::view-transition-*` pseudo-element tree) — see DECISIONS.md D-042

## Phase 10 — Account

- [ ] Guest checkout confirmed working end-to-end
- [ ] Account: profile, orders, addresses
- [~] Wishlist (anonymous local + merge on login) — the anonymous-local half is real: versioned `localStorage` (`lib/product/wishlist.ts`, mirroring `lib/product/recently-viewed.ts`'s D-031 pattern), a real PDP toggle (DESIGN_SYSTEM.md §42's right-panel "wishlist" action, unconditional even when sold out), a dedicated noindexed `/wishlist` route (Footer-linked, empty state `NOTHING SAVED YET.` per CONTENT.md §7), and a live sold-out-badge re-fetch reusing D-060's `GET /api/products/[handle]`. `add_to_wishlist` (GA4's own standard event) is wired, resolving PROJECT.md §82's previously-unattached "Wishlist adds." Merge-on-login stays blocked, unchanged: it needs a real Shopify store and a real customer-accounts system, neither of which exists yet (same D-016-class dependency as cart/checkout and the rest of this phase). See DECISIONS.md D-061.

## Phase 11 — Archive & Sold-Out States

- [ ] Sold-out product treatment (SOLD OUT / ARCHIVED, page stays accessible)
- [x] Archive foundation (Collection 001 only for now; structure ready for future drops) — real, working `/archive` (index) and `/archive/[handle]` (one archived collection's products) routes, both querying `getCollections`/`getCollection`/`getProductsByCollection` for real (their first real consumers since Phase 2) rather than stubs. Archived-vs-current is determined by real Shopify metafields (`custom.drop_status`/`custom.archived_at`, either signal sufficient — `lib/archive/collections.ts`'s `isCollectionArchived`), never fabricated. Today, honestly, that resolves to zero archived collections — `/archive` correctly renders CONTENT.md §7's `THE ARCHIVE BEGINS HERE.` rather than inventing an entry. A collection that exists but isn't archived (the real, current Collection 001) correctly 404s at `/archive/[handle]` exactly like a nonexistent handle, rather than rendering with a fabricated purchase-blocked state — verified live, not just read from the code, the single most safety-critical property of this feature. Purchasing controls on an archived product are the literal word `UNAVAILABLE` (PROJECT.md §41), architecturally distinct from the live PDP's inventory-driven `SOLD OUT`/`NO LONGER AVAILABLE.`: archived status is a property of the *collection*, never of a product's own remaining stock, so nothing in this view needs or fetches variant/inventory data at all. Discoverable via a real Footer link (Shopping nav, alongside Wishlist) rather than the primary Header/MENU nav, matching PROJECT.md §11's own explicit "Archive... may initially live under Collections" sequencing rather than jumping ahead of it; Homepage Scene 07 stays correctly omitted (DESIGN_SYSTEM.md §36's own "minimal or hidden" allowance). Both routes are indexed (unlike `/wishlist`) and `/archive` is in `app/sitemap.ts`; `/archive/[handle]` entries are not (no enumerable real handles exist yet, same honest limitation as `/products/[handle]`). Demo Mode extended in the same pass (`getCollection`/`getCollections`/`getProductsByCollection` had zero prior coverage) so the populated state is genuinely, verifiably previewable — confirmed live via direct `curl` checks, not merely asserted. Ships with zero net-new client-side JavaScript. Left deliberately out of scope, honestly: a collection hero/campaign image field (no spec basis beyond the still-omitted Scene 07), archive-specific analytics events (no discrete interaction to attach one to yet), and a permanent, committed E2E test of the populated path (blocked by the same no-real-Shopify-store limitation every catalog-facing phase has hit — verified instead via Demo Mode + live `curl`, matching D-057/D-058/D-060's established technique). See DECISIONS.md D-062 for the full design, an independently-found-and-fixed query-completeness gap, and an independently-found-and-fixed avoidable-waterfall fix.
- [ ] Upcoming-drop system (countdown/silhouette treatment) — foundation only, no real V2 drop yet

## Phase 12 — Launch Validation

- [~] Accessibility pass (WCAG 2.2 AA, keyboard, screen reader, reduced motion) — a verification/completeness pass over Phases 0-9 plus the just-finished performance pass, not new-feature work. The concrete starting point (`EntranceMotion.tsx`'s reduced-motion hydration-mismatch warning, flagged and deferred by D-044) was root-caused live and fixed (D-047: `motion/react`'s own `useReducedMotion()` isn't SSR-hydration-safe; replaced with a local `useSyncExternalStore` hook, the same pattern D-031 already established). A systematic check across keyboard operability, screen-reader semantics/headings/landmarks, color contrast, reduced motion, and forms found and fixed: a systemic `--color-esque-text-muted` contrast failure at five call sites (D-048); a missing live-region announcement for the Interactive Model's panel swap (D-049); missing field-level `aria-invalid`/`aria-describedby` on both Access Gate forms (D-050). An independent `ui-reviewer` pass (D-037/D-043's established convention) then live-drove the integrated result and found six further genuine defects — a `RecentlyViewed.tsx` hydration-safety bug in the same class as D-047, a keyboard-focus-management gap in the Access Gate's screen toggle, three checkboxes under WCAG 2.5.8's touch-target minimum (including a root-caused flexbox-shrink bug, not just a missing size class), and an unannounced PDP price/scarcity update — all fixed (D-051), plus two prior contrast judgment calls independently reconfirmed correct and one native-`<dialog>` platform quirk confirmed as expected browser behavior, not a defect. Left `[~]` rather than `[x]`, honestly: `ShopTheLookPanel`'s multi-item live-region refinement is deliberately deferred (needs per-row design, not a mechanical copy of the panel-wide pattern); `RequestAccessForm`'s new success-state `role="status"` fix could not be live-verified (Klaviyo is unconfigured in this environment, the same D-016/D-020 gap); testing was Chromium-only; and this pass's own systematic check, while covering every item this task named, is not an exhaustive audit of every WCAG 2.2 AA success criterion. See DECISIONS.md D-047 through D-051 for full findings, root causes, fixes, and verification methodology.
- [~] SEO pass (metadata, structured data, sitemap, Open Graph, canonical URLs) — every route's title/description/canonical/Open Graph now real (`lib/seo/metadata.ts`'s shared `buildPageMetadata`, canonicalizing category pages to their bare path regardless of Sort/Availability/Price filter params, D-024); Product + BreadcrumbList JSON-LD on the PDP and all seven category/subcategory routes (`lib/seo/structured-data.ts`, verified against Google's current structured-data guidance — no fabricated reviews/ratings/SKU); a real, honest, brand-token-based Open Graph image (`app/opengraph-image.tsx`, `next/og`, no fake product photo); `app/sitemap.ts` (static routes only — product PDPs need enumerable Shopify handles, the same D-016/D-023-class limitation prior phases hit, honestly deferred) and `app/robots.ts` (allows crawling, doesn't disallow `/access` — current Google guidance against combining `noindex` with robots.txt `Disallow`). See DECISIONS.md D-053 for the full pass. D-026 (unknown-subcategory routes returning HTTP 200 + `noindex` instead of a hard 404) was re-investigated fresh with new evidence — current Next.js docs name a Proxy-based fix, tried and empirically disproven for this codebase (it requires Cache Components, a sweeping rendering-mode change disproportionate to this gap); still unresolved, mitigated by the confirmed-present `noindex` tag — see DECISIONS.md D-052. Left `[~]`, honestly: product PDPs aren't in the sitemap (no real store to enumerate from) and D-026's status-code gap persists (mitigated, not fixed).
- [~] Performance pass (Core Web Vitals, image loading, bundle size) — `/access`'s own recorded candidate fix is implemented: `EntranceMotion.tsx` now uses `LazyMotion`/`m`/`domAnimation` instead of the full `motion` component (D-044), measured (not assumed) via a manual chunk-diffing method since Next 16.3.1's Turbopack `next build` prints no per-route size table — the `/access`-specific chunk shrinks 126,023→79,644 bytes raw (−36.8%), 41,129→27,904 bytes gzip (−32.2%), with byte-identical SSR output confirmed before/after. A broader audit of the built-so-far surface (homepage, catalog, PDP, Interactive Model, access gate) covering image loading, font loading, other components' bundle footprint, client-component boundaries, and hydration cost found and fixed two more real issues: category grids' first (LCP-candidate) image was unconditionally lazy-loaded, now opt-in `priority` (D-045); `Footer` was needlessly swept into the storefront shell's client bundle on every page, now passed as a server-rendered prop instead of imported directly (D-046, −610 bytes raw/−114 bytes gzip on the shared shell chunk). Font loading (`next/font/google`, self-hosted, already-correct `display`/`preload` defaults), `remotePatterns`, and `next/image` usage elsewhere were checked and found already correct — not touched. Current-state Lighthouse lab snapshots (`/access`, `/`; default mobile-simulated throttling): performance score 96/100 on both, TBT 10-11ms, CLS 0. **Deferred, not achievable here:** real field Core Web Vitals (LCP/INP/CLS measured against actual production traffic) — this environment has no real Shopify store and no real visitors, the same constraint every phase since Phase 2 has documented (D-016, D-025, D-033/D-034, D-038); the Lighthouse numbers above are lab/synthetic, not field data, and are honestly labeled as such. See DECISIONS.md D-044/D-045/D-046 for full methodology, numbers, and two pre-existing (unrelated, load-sensitive) Playwright test flakes found and characterized during validation.
- [~] Legal pages (Privacy, Terms, Shipping, Returns, Refunds) — all five built at `/legal/*`, plus an Accessibility Statement (`/legal/accessibility`) and a real `/contact` mechanism; Footer's three dead links (`/legal/privacy`, `/legal/terms`, `/contact`) fixed and extended to all seven pages; real SEO metadata and sitemap entries; Playwright coverage for routing/rendering/metadata/nav integration. Left `[~]`, honestly, for the same reason as this phase's other lines: the five policy pages are real page structure wrapped around content that is visibly, mechanically marked `[Placeholder]` wherever it depends on a business decision this project hasn't made yet (shipping carrier/rates, return window, exchanges, governing jurisdiction, minimum transacting age) — PROJECT.md §85 itself defers exact legal language to counsel, which is outside what an engineering pass can resolve. Cookie Policy is folded into Privacy rather than built standalone (no real tracking beyond the access-gate cookie exists yet to describe). See DECISIONS.md D-054.
- [~] Analytics wired (GA4, Search Console, Shopify Analytics, access funnel events) — a real, typed, closed event taxonomy (`lib/analytics/events.ts`) and a safe, env-gated GA4 adapter (`lib/analytics/gtag.ts`, `@next/third-parties`'s `GoogleAnalytics`, confirmed the current version-matched mechanism) are built and unit-tested; real `trackEvent` calls are wired at every interaction point this codebase can currently produce: PDP product view/Add to Bag/variant selection, the Interactive Model's hotspot selection/Shop the Look open/Quick Add/variant selection, category nav clicks, and the full Access Funnel (gate view, Request Access open/attempt, successful access — the last via a small new short-lived signal-cookie mechanism, DECISIONS.md D-055, that deliberately avoids regressing the storefront's static rendering). Search Console gets a real, working `<meta name="google-site-verification">` mechanism (`Metadata.verification.google`, gated on a new env var); Shopify Analytics is confirmed genuinely automatic (no cart/checkout exists yet to instrument, per D-016). Left `[~]`, honestly, for the same class of reason as every other Phase 12 line: no real GA4 property or Search Console account exists to verify actual data ingestion against, catalog filter/sort usage is deliberately left to GA4's own automatic page-view+query-string tracking rather than a bespoke event (preserving D-024's zero-JS filter architecture), and Shop the Look's own "purchase" click and homepage category-showcase clicks were scoped out this pass — see DECISIONS.md D-055 for the full reasoning on both.
- [~] Cross-device QA (mobile/tablet/laptop/desktop) — the last item in the phase sequence. `playwright.config.ts` gained two new projects (`tablet`: `devices['iPad Mini']`, 768×1024 WebKit/touch; `laptop`: Desktop Chrome engine at a custom 1024×768), giving all four of DESIGN_SYSTEM.md §14/§15's documented breakpoint tiers (mobile/tablet/desktop/large-desktop) real project coverage for the first time — previously only mobile-safari (390px) and chromium (verified, not assumed, to already be exactly 1280×720 — the "large desktop" tier) existed, leaving the entire 768-1279px band untested. A full hands-on audit (every real reachable route, plus a temporary fixture probe for the Shopify-gated grid/PDP/Interactive Model/dialogs, mirroring D-045/D-048/D-053's established technique) found the core responsive mechanisms — the 12/8/4-column grid transition, the PDP's 1024px two-column threshold, Interactive Model hotspot/panel layout, dialog side-panel thresholds, Header's text/icon threshold, and WCAG 2.5.8 touch targets — already exactly correct at the newly-covered tiers, with no D-014-class regression found; one incidental, pre-existing (Phase 3-era), already-designed-for `FullScreenMenu` short-viewport scroll characteristic was confirmed unchanged and not worsened by the new tiers. A new permanent `tests/e2e/responsive-audit.spec.ts` pins the newly-verified, genuinely cross-cutting assertions (per-project-viewport horizontal overflow, Header's md+ threshold, a real-touch-project WCAG 2.5.8 check) going forward. See DECISIONS.md D-056 for the full audit, every finding, and its methodology. Left `[~]`, honestly, for the same class of reason as every other line in this phase: real physical tablet/laptop hardware testing is categorically unavailable in this environment — Playwright's viewport/device emulation verifies real CSS layout response and real `hasTouch`/pointer-media outcomes (confirmed to match real device profiles), but not real touch-hardware nuance, physical rendering-engine quirks, true screen size/DPI, or a tablet used with an attached mouse (a fine pointer at a tablet-sized viewport) as a single tested target — see D-056 for the full, itemized limitation.

---

## V1.1 (post-launch follow-ups)

- Inventory reservation during checkout, if Collection 001 demand makes it necessary ([PROJECT.md §49](./PROJECT.md#49-inventory-reservation))
- Back-in-stock notifications (`NOTIFY IF RETURNED`)
- Klaviyo upgrade off free tier if subscriber base outgrows it
- Error monitoring / frontend error reporting once real customer traffic exists
- Behavioral analytics (Clarity or equivalent) if navigation confusion needs investigating

## V2+ (intentionally deferred — do not build yet)

Per [PROJECT.md §92](./PROJECT.md#92-deferred-v2):

- Full VIP membership / Esque Private / invitation codes / loyalty tiers
- Extensive Lookbook, expanded About editorial
- Mobile application
- Advanced 3D garments, 360° product photography, NFC garments
- Digital authentication / resale verification
- Member events, advanced outfit builder
- International shipping

## Open Product Decisions (not engineering blockers, but needed before launch)

Per [PROJECT.md §101](./PROJECT.md#101-items-still-intentionally-open) — track and resolve before Collection 001 goes live:

- Final logo, tagline, fonts, exact forest green value
- Product names, prices, sizes, colors, materials, fit descriptions, inventory quantities
- Photography style, model casting, image ratios, campaign direction
- Shipping carrier, price, free-shipping threshold, fulfillment provider, packaging, return window
- Collection 001 name, launch date/time, drop frequency, early-access duration
</content>
